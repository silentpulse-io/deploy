apiVersion: v1
kind: Service
metadata:
  name: silentpulse-notifications
  namespace: {{ .Values.global.namespace }}
  labels:
    app.kubernetes.io/name: silentpulse-notifications
    {{- include "silentpulse.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.notifications.port }}
      targetPort: {{ .Values.notifications.port }}
      protocol: TCP
  selector:
    app.kubernetes.io/name: silentpulse-notifications
