POSTGRES_DB=silentpulse
POSTGRES_USER=silentpulse
POSTGRES_PASSWORD=silentpulse
POSTGRES_PORT=5432
REDIS_PORT=6379
REDIS_PASSWORD=
JWT_SECRET=dev-only-jwt-secret-min-32-chars!
JWT_EXPIRY=24h
ENCRYPTION_KEY=
NOTIFICATION_POLL_INTERVAL=30s
CONNECTOR_HUB_URL=

# Container images — override for custom registries or pinned versions
IMAGE_REGISTRY=ghcr.io/silentpulse-io
IMAGE_TAG=latest

# MCP Server (optional — enable with: docker compose --profile mcp up -d mcp)
MCP_TENANT_ID=a0000000-0000-0000-0000-000000000001
MCP_API_KEY=  # generate with: openssl rand -hex 32
